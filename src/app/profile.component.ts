import { Component } from "@angular/core";

@Component({
  // selector: 'app-profile', Type selector
  // selector: '[appProfile]', attribute selector
  template: `
    <p>Profile component 1</p>
  `,
  standalone: true
})

export default class ProfileComponent {}